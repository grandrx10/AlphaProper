export class ItemFrame {
    constructor(item, slot, x, y, length, width){
        this.item = item;
        this.slot = slot;
        this.x = x;
        this.y = y;
        this.length = length;
        this.width = width;
    }
}