export class SimpleBullet {
    constructor (x, y, colour, length, width) {
        this.x = x;
        this.y = y;
        this.colour = colour;
        this.length = length;
        this.width = width;
        this.r = length;
    }
}